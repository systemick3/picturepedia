// Image crop functionality

// Fade in all images using vue js

// Allow for a caption

// Create git repository

// Instagram upload (NOT POSSIBLE)

// Twitter upload

// Facebook upload

// Create relationships so that users can follow each other

// Make profile pic croppable.

// Make profile editable.

// Prevent edit profile from creating duplicate email.

// Refactor user code to check for current user.

// Use user id instead of username in all admin URLs.

// Edit existing avatar functionality.

// Add description to user.

// Hashtags

// Add first name and last name to user profiles.

// all.js should be all js - need a separate file for cropping javascript.

// Like functionality

// Basic Comment functionality.

// Display multiple messages after upload.

// Look at messageBag for error messages when uploading. (NOT SUITABLE).

// Add user's own pictures to timeline.

// Validate uploaded file has correct extensions and mimetype and size.
// Validate filesize (What does Instagram do?)
// What to do about tiny files?
// What to do about large files

// Make avatar upload and crop use same views as upload/crop picture.

// Add items to main menu.

// Enforce limit of 255 chars on posts and comments.

// Refactor textarea js so component code is in separate file.

// On upload convert image to different sizes.

// Responsive images.

// Prevent Users deleting when avatar is deleted.

// Different sized images on profile and hashtag pages.
// 150
// 240
// 320
// 480
// 640
// 1080

// Add time of post eg 5 hours ago.

// Users must be able to delete a post.

// Deletion of comments.

// Delete old avatar when new one is added.

// Hashtags table & functionality.

// Search functionality.
// Hashtags table with count.
// Ajax call to get hashtags and users.
// Autocomplete dropdown.

// Front end js for multiple images in same post.

// Delete method on file?

// Handle upload of multiple images in one post.

// Show thumbnails of uploaded files on share page.


################# BACK END #############################

Write tests.

Sort upload messages.

Discs for files to show which image is being viewed.

Put 1080 images in 1080x1080 directory.

Page to display full size image in lightbox.

Check all doxygen @param & @return.

Should we store a file/avatar/upload before it has been cropped?
Should the old file/avatar be deleted after it is cropped?

Add a page/lightbox for upload guidelines (Google 'Instagram max size').

Terms and Conditions page.

Emails when joining.

Create follow button.
Create Edit Profile button.

Linkedin upload??

Public & private profiles.

Queue jobs to push to FB, Insta, Twitter Google

When iterating thought user's images should I use 'include' in template.

Ensure usernames are unique (create unique index and do check on account creation)

Make a composite class to handle files and images (contain Image and File class)

New homestead environment.

Login with facebook.

Login with Twitter.

Store Twitter/Facebook tokens to prevent having to login again.

Ensure that only posts/files with a status of 1 are ever selected.

Reformat code:
All functions to have drop down bracket.
All tabs to be 2 spaces.
Doxygen comments on all functions.
Use PSR2 coding standards.

Rationalise route/controller/view names.

Handle posts that don't contain files. (unlikely but could happen - check for file before displaying post);

################# FRONT END ############################

Create custom 404 & 403 pages.

Continuous scroll on homepage.

Find default image for avatars.

Drag and drop image on form.

Hashtag suggestions when typing in caption or search.

Remove everything except images from profile and hashtag pages.
